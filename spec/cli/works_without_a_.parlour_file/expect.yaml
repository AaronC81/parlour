success: true
files:
  rbi/works_without_a_.parlour_file.rbi: |
    # typed: strong
    module A
      extend T::Sig

      sig { returns(Integer) }
      def a; end
    end

    module B
      extend T::Sig

      sig { returns(String) }
      def b; end
    end
